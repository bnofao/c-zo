type ApiKey {
  id: ID!
  name: String
  prefix: String
  start: String
  enabled: Boolean!
  expiresAt: DateTime
  lastRequest: DateTime
  createdAt: DateTime!
}

extend type Query {
  myApiKeys: [ApiKey!]!
}
