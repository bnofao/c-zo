import type { ProductImageResolvers } from './../../types.generated'

export const ProductImage: ProductImageResolvers = {
  // ProductImage doesn't have any nested fields that need resolution
  // All fields are already available from the parent object
}
