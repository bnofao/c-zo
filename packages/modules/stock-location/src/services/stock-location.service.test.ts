import { describe, it } from 'vitest'

// TODO: Rewrite tests for the new service layer.
// Validation (handle, countryCode, slugify) is now in the GraphQL middleware (create-stock-location.ts).
// The service is a generic CRUD via buildService — test the middleware instead.

describe('stockLocationService', () => {
  it.todo('should be tested via the GraphQL middleware')
})
